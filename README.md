# WinShield-Scanner
WinShield Scanner is a single-file PowerShell script that audits local Windows security configurations and generates a standalone, dark-themed HTML dashboard. It functions strictly as a posture auditor, not an exploit tool or CVE scanner.
